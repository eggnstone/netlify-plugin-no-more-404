export class Store
{
    constructor(params: {configName: string; cwd: string})
    {

    }

    get(cacheKey: string):string[]
    {
        return [];
    }

    set(cacheKey: string, newCollection: string[])
    {

    }
}
